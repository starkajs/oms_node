extends layout

block content
    .container
        - const upName = name.toUpperCase();
        - const todayDate = Date.now();
        h2 Optimum Management System
        p#welcome Hello! #{upName}
        h2 Sale ends #{h.moment().endOf('day').fromNow()}
        span Yo!!! #{dog} - the time is #{todayDate}
        img(src="dog.jpg" alt=`Dog ${dog}`)